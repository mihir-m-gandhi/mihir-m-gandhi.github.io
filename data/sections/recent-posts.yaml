# section information
section:
  name: Articles
  id: recent-posts
  enable: false
  weight: 12
  showOnNavbar: false
  # Can optionally hide the title in sections
  # hideTitle: true

# no additional configuration is required
